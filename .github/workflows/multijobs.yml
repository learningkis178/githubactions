name: githubactions
run-name: ${{ github.actor }} is testing out GitHub Actions 
on: [push]
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - name: first_job
        RUN: "echo first stage"
  job2:
    runs-on: ubuntu-latest
    steps:
      - name: second_job
        RUN: "echo seconf job" 
         
    