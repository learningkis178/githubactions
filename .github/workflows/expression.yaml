name: checking the expression
on: [push]
env:
  name: orange
jobs:
  expression:
    runs-on: ubuntu-latest
    steps:
      - name: expression
        run: echo "access the expression ${{ env.name }}" 
      - name: check the run id
        run: echo"check the run id $GITHUB_RUN_ID"     